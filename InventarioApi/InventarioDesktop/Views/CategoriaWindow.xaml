<Window x:Class="InventarioDesktop.Views.CategoriaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Categorías" Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        MinHeight="400" MinWidth="450">
    
    <Grid>
        
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FAF8F5" Offset="0"/>
                <GradientStop Color="#F5F0EB" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <Ellipse Width="50" Height="50" Margin="0,0,15,0">
                    <Ellipse.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#D4A574" Offset="0"/>
                            <GradientStop Color="#B8935E" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Categorías" 
                               FontSize="26" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource OnSurfaceBrush}"/>
                    <TextBlock Text="Administra las categorías de productos" 
                               FontSize="13" 
                               Foreground="#FF888888"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
            
            
            <Rectangle Grid.Row="1" 
                       Height="2" 
                       Margin="0,0,0,20">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="Transparent" Offset="0"/>
                        <GradientStop Color="#E5DDD5" Offset="0.5"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            
            
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="Lista de Categorías" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource OnSurfaceBrush}"
                               Margin="0,0,0,15"/>
                    
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding Categorias}" 
                             SelectedItem="{Binding SelectedCategoria}"
                             DisplayMemberPath="Nombre"
                             BorderThickness="0"
                             Background="Transparent"/>
                </Grid>
            </Border>
            
            
            <Border Grid.Row="3" 
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="10"
                    Padding="15"
                    Effect="{StaticResource ButtonShadow}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="➕ Agregar" 
                            Command="{Binding AddCommand}"
                            Padding="20,10"/>
                    <Button Content="🗑️ Eliminar" 
                            Style="{StaticResource ActionButtonStyle}" 
                            Command="{Binding DeleteCommand}"
                            Padding="20,10"/>
                    <Button Content="🔄 Actualizar" 
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Padding="20,10"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>