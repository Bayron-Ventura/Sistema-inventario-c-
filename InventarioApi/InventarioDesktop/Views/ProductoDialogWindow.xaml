<Window x:Class="InventarioDesktop.Views.ProductoDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Producto" Height="600" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" 
        ShowInTaskbar="False">
    
    <Grid>
        
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FAF8F5" Offset="0"/>
                <GradientStop Color="#F5F0EB" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock x:Name="txtTitulo"
                           FontSize="24" 
                           FontWeight="SemiBold" 
                           Foreground="{StaticResource OnSurfaceBrush}"/>
                <Rectangle Height="2" Margin="0,10,0,0">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#D4A574" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
            </StackPanel>
            
            
            <Border Grid.Row="2" 
                    Style="{StaticResource CardStyle}"
                    Padding="25">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        
                        <Label Content="Nombre del Producto" 
                               Foreground="{StaticResource PrimaryBrush}"
                               FontWeight="SemiBold"/>
                        <TextBox x:Name="txtNombre" 
                                 Margin="0,5,0,20"/>

                        
                        <Label Content="Descripción" 
                               Foreground="{StaticResource PrimaryBrush}"
                               FontWeight="SemiBold"/>
                        <Border BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="2"
                                CornerRadius="8"
                                Margin="0,5,0,20">
                            <TextBox x:Name="txtDescripcion" 
                                     Height="90" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     BorderThickness="0"
                                     Padding="12,10"/>
                        </Border>

                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="20"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            
                            <StackPanel Grid.Column="0" Grid.Row="0">
                                <Label Content="Categoría" 
                                       Foreground="{StaticResource PrimaryBrush}"
                                       FontWeight="SemiBold"/>
                                <ComboBox x:Name="cmbCategoria"
                                          Margin="0,5,0,0"
                                          DisplayMemberPath="Nombre" 
                                          SelectedValuePath="Id"/>
                            </StackPanel>

                            
                            <StackPanel Grid.Column="2" Grid.Row="0">
                                <Label Content="Stock" 
                                       Foreground="{StaticResource PrimaryBrush}"
                                       FontWeight="SemiBold"/>
                                <TextBox x:Name="txtStock" 
                                         Margin="0,5,0,0"/>
                            </StackPanel>

                            
                            <StackPanel Grid.Column="0" Grid.Row="3">
                                <Label Content="Precio" 
                                       Foreground="{StaticResource PrimaryBrush}"
                                       FontWeight="SemiBold"/>
                                <TextBox x:Name="txtPrecio" 
                                         Margin="0,5,0,0"/>
                            </StackPanel>

                            
                            <StackPanel Grid.Column="2" Grid.Row="3">
                                <Label Content="Fecha de Creación" 
                                       Foreground="{StaticResource PrimaryBrush}"
                                       FontWeight="SemiBold"/>
                                <TextBox x:Name="txtFechaCreacion" 
                                         Margin="0,5,0,0"
                                         IsReadOnly="True"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            
            <Border Grid.Row="3" 
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="10"
                    Padding="15"
                    Margin="0,20,0,0"
                    Effect="{StaticResource ButtonShadow}">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button x:Name="btnAceptar"
                            Content="✓ Guardar" 
                            IsDefault="True" 
                            Click="Aceptar_Click"
                            Padding="25,10"
                            Margin="0,0,10,0"/>
                    <Button x:Name="btnCancelar"
                            Content="✕ Cancelar" 
                            IsCancel="True"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Padding="25,10"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>